# 部署说明（阶段 0 · 环境联通）

> 环境 ID：`nno-d2gspwvpl6c3c9f46`（地域 ap-shanghai）
> 计费版本：**包月体验版 —— 无静态网站托管**，Web 管理端由 HTTP 云函数 `admin-static` 托管
> 命令请在 **cmd** 中执行（项目根目录 `E:\selftext\skillswap`）

---

## 一、为什么需要你来执行部署

当前沙箱环境无法访问腾讯云 API（`tcb.tencentcloudapi.com` 域名解析被拦截），
所以 **代码我已写好并本地验证通过，部署命令需要你在本地 cmd 执行**。
下面每一条都是可直接复制的命令。

---

## 二、一次性准备（CloudBase 控制台 · 只需做一次）

### 1. 创建数据库集合（4 个）

控制台 → 数据库 → 新建集合：

```
users       用户
posts       技能帖
exchanges   交换关系
configs     配置（管理员登录失败计数）
```

### 2. 配置安全规则（4 个集合统一）

数据库 → 集合 → 权限设置 → 自定义安全规则：

```json
{ "read": false, "write": false }
```

> 这是「前端不直连数据库」的强制保障：端侧即使拿到环境 ID 也读不到任何数据。

### 3. 配置索引

按 `cloudbase/db/indexes.md` 第 2 节表格逐个添加（users 2 个、posts 2 个、exchanges 3 个）。

### 4. 创建云函数并开启 HTTP 访问服务

| 函数名 | 运行时 | 内存 | 超时 | HTTP 访问服务 |
|---|---|---|---|---|
| `skillswap-api` | Nodejs 18 | 256MB | 20s | **开启** |
| `admin-static` | Nodejs 18 | 256MB | 10s | **开启** |

### 4b. HTTP 网关添加路由（**必做**，否则外网访问不了）

> 包月体验版下，云函数**不会**自动获得外网地址，必须到
> **HTTP 网关 → 路由管理** 手动添加路由，把路径指到云函数。

路由表（**默认域名** 分组下添加，域名选 `nno-d2gspwvpl6c3c9f46-1479540360.ap-shanghai.app.tcloudbase.com`）：

| 路径 | 资源类型 | 资源对象 | 路径透传 | 跨域 | 认证 |
|---|---|---|---|---|---|
| `/skillswap-api/` | 云函数 | `skillswap-api` | **关闭** | 关闭 | 关闭 |
| `/skillswap-web/` | 云函数 | `admin-static` | **关闭** | 关闭 | 关闭 |

> **路径透传一定要关闭**：关闭时网关会剥掉前缀，云函数收到的 path 是
> `/api/health` 而不是 `/skillswap-api/api/health`，正好命中代码里的路由。

**已确认可用的默认域名**：

```
https://nno-d2gspwvpl6c3c9f46-1479540360.ap-shanghai.app.tcloudbase.com
```

格式是 `<envId>-<创建时间戳>.ap-shanghai.app.tcloudbase.com`。
⚠️ 常见错误：写成 `<envId>.service.tcloudbase.com`（不存在），
或在中间多插一段（如 `…-c9f46-…`）→ 网关返回 `INVALID_ENV`。

最终两个入口：

```
API： https://nno-d2gspwvpl6c3c9f46-1479540360.ap-shanghai.app.tcloudbase.com/skillswap-api
Web： https://nno-d2gspwvpl6c3c9f46-1479540360.ap-shanghai.app.tcloudbase.com/skillswap-web/
```

### 5. 配置云函数环境变量（`skillswap-api`）

> 入口：云函数 → `skillswap-api` → 函数配置 → 环境变量 → 添加。
> 改完环境变量**必须重新部署一次**函数才会生效。

| 变量 | 说明 | 取值来源 | 状态 |
|---|---|---|---|
| `WX_APPID` | 小程序 AppID | 微信公众平台 → 开发 → 开发管理 | ☐ 待填 |
| `WX_SECRET` | 小程序 AppSecret | 微信公众平台（重置后只显示一次） | ☐ 待填 |
| `TOKEN_SECRET` | 自签 token 密钥 | `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"` | ☑ 已生成 |
| `ADMIN_USER` | 管理员账号 | 填 `admin` | ☑ 已定 |
| `ADMIN_PASS_HASH` | 口令哈希（非明文） | `node scripts/gen-admin-hash.js Admin@12345` | ☑ 已生成 |
| `TMS_SECRET_ID` | 内容安全密钥 ID | 你已准备好，填入后 G-03 AI 预审走真实 TMS（不填则降级为本地词表） | ☑ 已就绪待填 |
| `TMS_SECRET_KEY` | 内容安全密钥 Key | 同上 | ☑ 已就绪待填 |

> 不填 TMS 时系统自动降级为「仅本地敏感词表」，不影响发布功能（Z-06 仍可用）。

**口令哈希格式**（`lib/password.js` 校验规则）：

```
pbkdf2$<迭代次数>$<salt-hex>$<hash-hex>
```

四段，前缀必须是 `pbkdf2`，salt 16 字节（32 hex），hash 32 字节（64 hex，PBKDF2-SHA256）。
已实测：默认口令 `Admin@12345` 校验通过，错误口令与空值均正确拒绝。

### 6. 小程序后台配置 request 合法域名

微信公众平台 → 开发 → 开发管理 → 服务器域名 → request 合法域名，加入：

```
https://nno-d2gspwvpl6c3c9f46-1479540360.ap-shanghai.app.tcloudbase.com
```

开发期可在开发者工具「详情 → 本地设置」勾选 **不校验合法域名** 先联调；
**真机预览前必须配好**，否则请求失败。

---

## 三、部署（cmd 执行）

```bat
cd /d E:\selftext\skillswap

:: 1. 登录（会打开浏览器扫码）
tcb login

:: 2. 确认环境
tcb env list

:: 3. 部署业务 API 云函数
cd cloudbase
tcb fn deploy skillswap-api -e nno-d2gspwvpl6c3c9f46

:: 4. 构建并拷贝 Web 管理端产物
cd ..\admin-web
npm run build:deploy

:: 4. 环境变量改动后，两个函数都要重新发一次
tcb fn deploy skillswap-api -e nno-d2gspwvpl6c3c9f46
tcb fn deploy admin-static -e nno-d2gspwvpl6c3c9f46

:: 5. 校验环境变量是否生效（configured 里应全为 true，TMS 除外）
curl https://nno-d2gspwvpl6c3c9f46-1479540360.ap-shanghai.app.tcloudbase.com/skillswap-api/api/health
```

> 若 `cloudbaserc.json` 里已写好 envId，命令中的 `-e` 可省略。

---

## 四、阶段 0 验收清单（逐条打勾）

打开浏览器/小程序依次验证：

- [x] **① 健康检查**：访问
      `https://nno-d2gspwvpl6c3c9f46-1479540360.ap-shanghai.app.tcloudbase.com/skillswap-api/api/health`
      返回 `{"code":0,...}`（**已于 2026-09-09 通过**，返回 200 + 3 条路由注册成功）。
      待办：`configured` 里 `WX_APPID`、`WX_SECRET`、`TOKEN_SECRET`、`ADMIN_PASS_HASH` 目前全是 `false`，
      **配完环境变量后需再访问一次确认变为 `true`**；
      `TMS` 为 `false` 属正常，阶段 2/3 再填。

- [ ] **② 登录建号**：微信开发者工具打开 `miniprogram` 目录真机/模拟器运行，
      控制台 → 数据库 → `users` 集合出现一条新记录（`_openid` 有值）

- [x] **③ 管理端可打开**：
      `https://nno-d2gspwvpl6c3c9f46-1479540360.ap-shanghai.app.tcloudbase.com/skillswap-web/`
      出现登录页（红底「技」方块 + 技能交换平台 + 管理端 · 管理员登录），样式应与原型图 5-7 一致
      （**已于 2026-09-09 通过**：index.html 420B、JS 94KB、CSS 2.1KB 均正常返回 200）

- [ ] **④ 端侧直连被拒**：在小程序里临时写一段 `wx.cloud` 之外的直连代码不可行；
      改为在控制台「数据库 → 权限设置」确认规则为 `{read:false,write:false}` 即可

- [ ] **⑤ 无跨域报错**：管理端打开后 F12 控制台无 CORS 错误（同源同域名）

---

## 五、常见问题

| 现象 | 原因 | 处理 |
|---|---|---|
| `tcb fn deploy` 报 No valid identity | 未登录 | 先 `tcb login` |
| 访问 health 返回 404 | HTTP 访问服务未开启，或路径不对 | 控制台开启；路径为 `/skillswap-api/api/health` |
| 管理端打开是空白 | `dist/` 未随函数部署 | 重新 `npm run build:deploy` 后再 `tcb fn deploy admin-static` |
| 管理端资源 404 | `vite.config.js` 的 `base` 被改成了 `/` | 必须是 `'./'` |
| 小程序请求失败 | 域名未加入 request 合法域名 | 见二.6，或开发期勾选「不校验合法域名」 |
| 登录返回「服务端未配置 WX_APPID」 | 环境变量没配 | 见二.5（改完环境变量需重新部署函数） |

---

## 六、改前端样式后的正确流程

**不是热更新**，必须重新构建并重新部署：

```bat
cd admin-web
npm run build:deploy
cd ..\cloudbase
tcb fn deploy admin-static
```

---

## 七、当前进度

- [x] 后端骨架（路由 / 统一响应 / token / 数据库封装 / 敏感词 / TMS 签名）
- [x] Z-01 登录建号、Z-02 资料维护（`/api/auth/login`、`/api/auth/me`、`/api/auth/profile`）
- [x] 小程序端：request 封装、登录态、广场页、「我的」入口页、个人信息页
- [x] Web 管理端：登录页 + 后台框架（Vue3 手写样式）
- [x] 阶段 2：广场列表、发布与内容安全（Z-03～Z-06、Z-12）—— 22 条逻辑单测全绿
- [x] 阶段 3：管理端审核闭环（G-01 登录锁定 / G-02 待审列表 / G-03 AI 预审 / G-04 通过驳回，G-05 已审记录复用 G-02 状态筛选）—— 19 条逻辑单测全绿
- [x] 阶段 4：交换闭环（Z-08 发起 / Z-09 收到与确认驳回 / Z-10 我的交换 / Z-11 完成与互评）—— 23 条逻辑单测全绿
- [x] 阶段 5：用户管理（G-06 用户搜索 + G-07 用户详情/发布/交换 + G-08 封禁解封连带帖子下架/恢复）—— 14 条逻辑单测全绿
- [ ] 阶段 6：选做（Z-07 推荐）、打磨与交付

> 本地逻辑层测试命令：`node tests/stage2.test.js`、`node tests/stage2b-hybrid.test.js`、`node tests/stage3.test.js`、`node tests/stage4.test.js`、`node tests/stage5.test.js`
> （测试用内存数据库 + mock 微信/TMS，无需联网或真实云资源，已覆盖越权、重复、冲突、降级等关键路径；全套 **82 条**逻辑单测）

## 八、审核模式决策（2026-09-10 确定）

采用 **混合审核模式**（用户拍板，替代初期的严格模式）：

- 本地敏感词表命中 → 直接拦截（不发）
- TMS = `Block` → 直接拦截（不发）
- TMS = `Pass` → **直接 `passed` 上广场，免人工**
- TMS = `Review`（疑似）→ `pending` 进人工审核队列，管理员 G-04 通过后才上广场
- TMS 未配置 / 调用异常（降级）→ `pending` 进人工审核队列（安全优先）

实现位置：`cloudbase/functions/skillswap-api/routes/posts.js` 的 `decideStatus()`，`create()` 与 `republish()` 共用。
验证：`tests/stage2b-hybrid.test.js`（4 条，覆盖 Pass/Review/Block/重提）。

> 注意：`decideStatus` 依赖 `config.isTmsEnabled()` 在 require 时缓存的环境变量，
> 因此改 TMS 密钥后必须重新 `tcb fn deploy skillswap-api` 才生效。

---

## 九、测试数据灌库（150 条各类型基底）

> 用途：一键灌入「用户 22 + 帖子 100 + 交换 28」共 150 条校园场景数据，便于验收广场、审核队列、交换闭环、发布后互补推荐、管理端看板。
> 数据定义：`cloudbase/functions/skillswap-api/lib/seed-data.js`
> 灌库接口：`POST /api/admin/seed`（管理员专属，`body:{reset?:boolean}`；`reset:true` 先清空三集合再写入）

**步骤（cmd 执行）：**

```bat
cd /d E:\selftext\skillswap

:: 1. 先部署最新 skillswap-api（含 /api/admin/seed 路由）
cd cloudbase
tcb fn deploy skillswap-api -e nno-d2gspwvpl6c3c9f46
cd ..

:: 2. 一键灌库（不传 --reset 为追加；传 --reset 先清空再写）
node scripts/seed-run.js --reset
```

灌库成功会打印：`已灌入测试数据（用户22/帖子100/交换28）`。
本地逻辑校验（无需联网）：`node tests/seed-data.test.js`（13 项全绿，覆盖计数、状态分布、引用完整性、teach/learn 成对、reset 幂等）。

**数据构成速览：**

| 类型 | 数量 | 状态/说明 |
|---|---|---|
| 用户 | 22 | 21 正常 + 1 封禁（seed_u12，用于测试 G-08 封禁联动） |
| 帖子 | 100 | passed 88 / pending 5 / rejected 4 / offline 3；每用户 4 条（2 teach + 2 learn），覆盖 6 大分类；热门标签 teach/learn 成对（发布后互补推荐可直接命中） |
| 交换 | 28 | pending 7 / active 8 / completed 10（带互评）/ rejected 3；均引用真实 passed 帖且 applicant≠target |

## 十、技能广场意图 Tab（2026-09-11 新增）

**交互结构**（两个维度正交，互不干扰）：

```
[全部]  [我想学]  [我能教]          ← 意图（对应 posts.type：all / learn / teach）
[全部分类] [学业辅导] [语言交流] …   ← 分类（对应 posts.category）
──────────── 信息流 ────────────
```

- **默认落点**：读本地缓存 `skillswap_intent`，无缓存/非法值回落「全部」，保护浏览型用户的信息密度；
- **数量徽标**：三个 Tab 各带一个数字（该分类下 passed 帖数），切换前就能判断「有没有我要的」；
- **空态转化**：无结果时给出双 CTA —— 主 CTA「发布我的需求 / 发布我能教的」跳发布页并**预选 type + category**（`/pages/publish/publish?type=learn&category=学业辅导`），次 CTA「看看谁在教 X」一键切到对侧 Tab，避免把用户堵死。

**新增接口**：`GET /api/posts/counts`（公开，只统计 `status=passed`）

| 参数 | 说明 |
|---|---|
| `category` | 可选，限定分类（切分类时徽标跟着变）；非法值返回 400 |

返回 `{ all, teach, learn }`。注意路由注册在 `/api/posts/:id` **之前**，否则会被 `:id` 误匹配。

**部署**：本次仅改动 `skillswap-api` 与小程序，管理端无需重新部署。

```bat
cd /d E:\selftext\skillswap\cloudbase
tcb fn deploy skillswap-api -e nno-d2gspwvpl6c3c9f46
```

小程序端在微信开发者工具**重新编译**即可（无需上传）。

## 十一、单账号交换验收 · 沙盒模式（2026-09-11 新增）

### 为什么需要

交换闭环天然需要**两个账号**（申请人 A、帖主 B），且每个关口都有身份校验：

| 环节 | 校验 | 单账号为什么过不去 |
|---|---|---|
| 发起申请 | 不能申请自己的帖 | 只有自己的帖可点，直接被 403 拦住 |
| 同意 / 拒绝 | 仅帖主可操作 | 帖主是别人 |
| 标记完成 | 双方各点一次 | 同一账号凑不齐两个人 |

所以只用一个微信号，**跑不通完整链路**。

### 两条路，任选

**① 推荐（最真实）：微信开发者工具「多账号调试」**
> 需要第二个微信号。在微信公众平台 → 成员管理里把另一个微信号加为「体验者 / 开发者」，然后在开发者工具里切换账号，一边当 A 一边当 B。

**② 兜底（零门槛）：打开沙盒模式，单账号自交换**

在云函数环境变量里加一条：

| 变量名 | 值 | 说明 |
|---|---|---|
| `SANDBOX_MODE` | `true` | 开启后允许「对自己的帖发起交换」，并在自交换时一次点击即完成 |

开启后单账号可以这样走完：

```
发布一条「我能教」帖
  → 进自己的帖子详情页，点「发起交换」（沙盒放开限制）
  → 「我的 - 收到的申请」里点「同意交换」→ 弹出安全提醒
  → 「我的 - 我的交换」里点「标记完成」→ 直接变为「已完成」
  → 评价「满意 / 不满意」→ 流程闭环
```

**怎么确认沙盒是否生效**：访问健康检查地址，看 `SANDBOX` 字段：

```
https://nno-d2gspwvpl6c3c9f46-1479540360.ap-shanghai.app.tcloudbase.com/skillswap-api/api/health
```

沙盒关闭时该字段为 `false`（默认），此时申请自己的帖仍返回 403。

> ⚠️ **上线前务必处理**：把云函数环境变量 `SANDBOX_MODE` 删除或改为 `false`，并重新 `tcb fn deploy skillswap-api`。
> 沙盒只放开「自己的帖」这一条限制，不影响他人数据；但保留着会让用户可以通过自交换给自己刷好评，因此只用于验收期。
> 另外，沙盒自交换的评价**不累计好评**（`selfSwap: true`），所以即使用户拿到了入口也刷不出分。

## 十二、管理端「所有帖子」+ 小程序自研选择器（2026-09-11 新增）

### 1. 管理端 · 所有帖子（G-09）

左侧导航「内容审核」下新增 **所有帖子**，用于全量检索与违规处置：

- **筛选**：状态（全部 / 待审核 / 已通过 / 已驳回 / 已下架）× 类型（我能教 / 我想学）× 分类（六大类）+ 关键词（模糊匹配 标题 / 正文 / 作者名），条件可任意组合；
- **操作**：待审帖可「AI 预审 / 通过 / 驳回」；已上广场的帖子可「下架」（需填原因）；已下架的可「恢复上架」；
- 列表一次拉 100 条（`size=100`），超出请用筛选缩小范围。

**新增接口**：

| 接口 | 说明 |
|---|---|
| `GET /api/admin/posts?status=all&keyword=&category=&type=&size=&cursor=` | `status=all` 表示不限状态；`size` 上限 100 |
| `POST /api/admin/posts/:id/takedown` | `body:{action:'offline'\|'restore', reason?}`；下架仅限 `passed`，恢复仅限 `offline` |

### 2. 小程序 · 自研下拉选择器 picker-sheet

**背景**：发布页「分类」原本用原生 `<picker>`。原生 picker 的弹层由微信客户端（iOS / Android 原生控件）渲染，**属于 Native 层，CSS 完全无法覆盖**；当手机系统开启深色模式时，弹层会渲染成深色，浅色页面里就出现"黑边"。

**方案**：新增 `components/picker-sheet/`（底部弹层选择器），样式全部由 `tokens.wxss` 控制，与设计语言一致，彻底不受系统深浅色影响。

```html
<picker-sheet
  show="{{catPickerShow}}" range="{{cats}}" value="{{catIndex}}" title="选择分类"
  bind:change="onCatChange" bind:cancel="onCatCancel"
/>
```
`change` 事件 `detail = { index, value }`。后续其他下拉场景（年级、性别等）可直接复用。

### 3. 交换安全提醒（Z-13）

- 帖主点「同意交换」成功后 → 自动弹出安全须知弹窗；
- 「我的交换」中**待开始 / 进行中 / 已完成**的卡片内 → 常驻安全提示条（点击可再看完整须知），双方都可见；
- 帖子详情页交换区 → 一行轻提示；
- 文案集中在 `miniprogram/utils/safety.js`（人身安全 + 财产安全 + 信息保护），便于统一修改。

### 3.1 开始协作与联系方式（Z-14）

交换进入「帖主已接受」后，卡片会依次出现三段内容：

1. **联系方式区**：双方互相可见（后端仅在 `active / started / completed` 下发 `peerContact / myContact`，
   `pending / rejected` 一律为空）；长按号码可复制。
2. **开始协作**：帖主确认后出现开始按钮 —— 教学者看到「教学开始」、学员看到「申请开始」；
   一方点过后，另一方按钮变为「确认开始」+ 改约按钮（学员侧「临时有事」、教学者侧「再约时间」），
   改约会通过 `POST /api/exchanges/:id/cancel-start` 给对方留一条留言并撤回本次开始。
   双方都确认后进入 `started`。
3. **标记完成 / 互评**：**双方都确认开始（`started`）之后**才出现「标记完成」；两人都标记完成 →
   `completed` → 收起操作按钮，进入互评，之后卡片直接展示我的评价内容（评分徽标 + 评语 + 时间）。
   还没开始就想终止这次交换，走下面的「3.3 取消交换」。

**帖子占用规则**（`lib/exchangeState.js` 实时推导，不冗余到 posts）—— **2026-09-14 修订**：

| 帖子上存在 | 其他人能否发起申请 | 帖主能否确认新申请 | 已有的「待开始」能否开始 |
|---|---|---|---|
| 无 / 仅 `pending` | ✅ | ✅ | — |
| `active`（已接受、待开始） | ✅ 不锁帖 | ✅ 随时可确认 | ✅ |
| `started`（双方已开始） | ✅ | ❌ 等这次结束后 | ❌ 等这次结束后 |
| `completed / cancelled / rejected` | ✅ | ✅ | ✅ |

一句话：**帖子始终开放申请，帖主也随时可以确认**；只有「进行中」才限制 —— 同一帖子同时只能有一摊真正进行中的交换。
前端据此禁用按钮：`POST /api/exchanges/:id/confirm` 与 `/start` 在 `postBusy` 时返回 409；
列表接口（`/api/exchanges`、`/api/exchanges/received`）每行下发 `postBusy` 供按钮置灰并给出原因。

> 「进行中」的占用还有**第二层维度：同一对用户**（见 3.3.1）——只按帖子判断是不够的。

### 3.2 广场标签搜索（2026-09-14）

- 首页顶部搜索框：`GET /api/posts?keyword=`（模糊匹配 **标签 / 标题 / 正文**，大小写不敏感；与 `type`、`category`、`cursor` 是 AND 关系）。
- 聚焦时展开「热门标签」建议：`GET /api/posts/tags?limit=12&category=`，点标签直接搜。
- 意图徽标 `GET /api/posts/counts` 同步支持 `keyword`，搜索时数字与列表一致。
- 搜索无结果时空态给出「清空搜索，看看全部」。
- 关键词统一走 `lib/search.js`（正则转义 + 20 字截断），管理端帖子搜索也复用它，并支持按标签搜。

### 3.3 取消交换与状态机（2026-09-14）

交换状态机共 6 个状态、5 条去向：

| 状态 | 含义 | 能做什么 | 去向 |
|---|---|---|---|
| `pending` | 已申请，待帖主处理 | 帖主：同意交换 / 拒绝 | → `active` / `rejected` |
| `active` | 帖主已接受，**双方还没开始** | 开始 · 临时有事/再约时间 · **取消交换** | → `started` / `cancelled` |
| `started` | 双方都点了「开始」，进行中 | **标记完成**（此时不能再取消） | → `completed` |
| `completed` | 双方都标记完成 | 互评（学员三选一 + 教学者写评语） | 终态 |
| `cancelled` | 双方都确认取消 | —（**直接跳过评价**，也不再互发联系方式） | 终态 |
| `rejected` | 帖主拒绝申请 | — | 终态 |

**按钮出现规则**（`pages/exchanges`，与后端校验一致）：

- `active`：`开始` + `临时有事 / 再约时间` + **`取消交换`**；此阶段**没有**「标记完成」。
- `started`：只剩 **`标记完成`**（两人都点 → `completed` → 正常进入评价）。
- 「取消交换」是**双方各确认一次**：我先点 → 卡片提示「你已申请取消，等对方确认」；
  对方看到「对方申请取消这次交换」，按钮变成「**确认取消**」；两人都确认 → `cancelled`。
- **开始与取消互斥**：点「开始」会清空 `cancelBy`、点「取消」会清空 `startBy`，
  不会出现「既申请开始又申请取消」的矛盾状态；任一方都能用「开始」把取消申请作废，反之亦然。
- 新增字段 `cancelBy[]`（已确认取消的人）、`cancelTime`。接口：
  `POST /api/exchanges/:id/cancel`（仅 `active` 允许，`pending` / `started` 一律 409）。
- 反向收紧：`POST /api/exchanges/:id/complete` 现在**只在 `started` 允许**（`active` 阶段 409，提示改用「取消交换」）。
- `cancelled` 之后：**不占用帖子**（`exchangeState.busy=false`，别人照常申请），且 `cancelled` 不参与去重
  （同一人可以重新申请该帖）。
- 列表下发：每行带 `cancelBy / iRequestedCancel / peerRequestedCancel`，前端据此切换按钮文案与提示。

### 3.3.1 同一对用户同时只能有一摊进行中（2026-09-15 缺陷修复）

**现象**：测试号 `testB` 申请了同一位帖主 `testA` 的两门课（两条不同的帖子），
两条都被帖主接受成「待开始」，随后各自点「开始」——结果**两摊都进入了「进行中」**，
「我的交换」里对同一个人出现两张「进行中」卡片，都带着「标记完成」按钮。

**根因**：原来的单摊约束只按**帖子**维度判断（`lib/exchangeState.of()` → `postId + status='started'`）。
两条帖是两个不同的 `postId`，因此互不拦截，`confirm` 与 `start` 全部放行。

**修复**：新增**用户对**维度（`lib/exchangeState.pairBusy()` / `busyPairMap()`，键为顺序无关的 `pairKey(a,b)`）：

| 动作 | 追加校验 | 冲突提示 |
|---|---|---|
| `POST /api/exchanges/:id/confirm` | `pairBusy(targetId, applicantId)` | 你和 TA 之间已有进行中的交换，等这次结束后再确认新的申请 |
| `POST /api/exchanges/:id/start` | `pairBusy(me, counterpart)` | 你和 TA 之间已有进行中的交换，先完成或取消这次再开始 |

- 两个维度**同时生效**：帖子维度拦「同帖的第二摊」，用户对维度拦「同一个人对同一人的第二摊」。
- 列表 / 详情新增下发 `peerBusy`（`pmap[pairKey(me, peerId)] && status !== 'started'`），
  前端 `pages/exchanges/exchanges.js#decorate` 据此把「同意交换 / 开始」置灰并给说明文案；
  与 `postBusy` 共用一个提示位，文案按维度区分（「这条帖…」/「你和 TA 之间…」）。
- **边界（刻意保留）**：
  - `active`（待开始）**不占用**用户对 —— 同一对用户可以先有多条待开始，谁先开由单摊约束决定；
  - 只约束**同一对**：甲与乙进行中时，甲与丙、乙与丁照常（帖主可以同时带多个学生）；
  - `pending` 申请入口不受限制（不能因为有一摊在跑就不让新同学投申请）；
  - **沙盒自交换不参与**（`applicantId === targetId` 时 `pairKey` 返回空串），
    否则单账号验收没法同时验两条自交换。
- 结束（`completed` / `cancelled`）后该用户对自动释放，另一摊即可正常开始。

### 3.4 结束后的「再次申请」与索引修正（2026-09-14 缺陷修复）

**现象**：交换完成后再申请同一帖，小程序整屏弹出数据库原始报错：

```
[FailedOperation.Insert] bulk write error: [[E11000 duplicate key error collection:
tnt-xxxx.exchanges index: postId_applicantId_unique dup key: { postId: "...", applicantId: "sandbox:testA" }]]
```

**根因**（两处叠加）：

1. `exchanges` 的 `postId + applicantId` 被建成了**唯一索引**；
2. `create()` 的去重判断是「按 postId+applicantId 查 `limit(1)` 一条，再在内存里看它是不是 pending/active/started」。
   交换完成后库里已有该对的旧记录，查到的正是这条 `completed`，于是**去重放行 → 插入被唯一索引顶掉**。

**修复**：

- 删除唯一索引 `postId_applicantId_unique`，改建**普通**复合索引 `postId_applicantId_status`（方向 1/1/1）。
  控制台路径：数据库 → exchanges → 索引管理；确认该索引 `Unique` 为 `false`。
- `routes/exchanges.js` 的去重查询把状态下推：
  `.where({ postId, applicantId, status: cmd().in(['pending','active','started']) })`。
- 插入包一层兜底：真被历史遗留的唯一索引拦下时，把 `E11000` 转成可读的 `409 你已对该帖子发起过申请`，
  不再把数据库原始堆栈抛给用户。

**最终规则**：同一对 (postId, applicantId) 同时只允许一笔**未结束**的交换；
`completed` / `cancelled` / `rejected` 之后都可以再次申请同一帖，生成新记录，旧记录保留作历史（互评各自独立计数）。

> 这只是业务层约束，**不要再把它做成数据库唯一索引**——一旦恢复唯一索引，本缺陷会立刻复现。

### 3.5 已评价后收起评价按钮 + 「openid / _openid」字段错位（2026-09-14 缺陷修复）

**现象**：交换完成后已经评价过，卡片上「不满意 / 暂不评价 / 满意」三个按钮仍然可点；
再点一次会被后端拦下并提示「你已评价过」，用户分不清到底评没评过。

**根因**：前端把「我是否已评价过」建立在一个读不到的字段上。

- 登录接口（`/api/auth/login`、`/api/auth/test-login`、`/api/auth/me`）通过
  `services/userService.js#publicFields` 下发用户，字段名是 **`openid`**
  （它把数据库里的 `_openid` 映射成了 `openid`）；
- 但 `pages/exchanges/exchanges.js` 与 `pages/detail/detail.js` 读的是 **`_openid`** →
  恒为 `undefined` → `me` 是空串；
- 于是 `evaluations.some(x => x.openid === me)` 永远为 false，`canEvaluate` 一直为 true。

**同一根因还牵连两处**（一并修好）：

- 详情页 `isMine` 永远为 false → **自己看自己的帖子也显示「发起交换」按钮**；
- 点自己头像进不了「我的」（走 `navigateTo` 而非 `switchTab`）。

**修复**：

- `utils/auth.js` 新增 `getMyOpenid()`，`openid` / `_openid` 两种字段名都兼容，
  所有读取点统一改用它（**不要再手写 `getCachedUser()._openid`**）。
- `decorate()` 的「已评价」改为**以后端下发的 `myEvaluation` 为主判据**，不再只依赖 openid 比对——
  即使 openid 解析为空，按钮也不会再收不起来。
- 后端 `decorateExchange` 增发 `peerEvaluation`，前端不用自己做 openid 比对。
- UI：我提交评价后收起评价按钮与安全提示条，改为「**已完成评价**」结果区，
  依次展示**我的评价**与**对方的评价**（评分徽标 + 评语 + 时间）；
  对方还没评价时显示「对方还没评价，评价完成后会显示在这里」，双方都评完提示「这次交换已结束」。

> 排查口径：任何「我是否已 X」的判断失灵，先打印登录返回的 `data.user`——
> 字段名是 `openid`。数据库里叫 `_openid`，接口下发时已经改名。

### 4. 本次部署

```bat
:: 后端（peerEvaluation 下发 + 结束后的再次申请修复 + 索引调整）
cd /d E:\selftext\skillswap\cloudbase
tcb fn deploy skillswap-api --force -e nno-d2gspwvpl6c3c9f46

:: 管理端（走 /skillswap-web/ → admin-static 云函数这条链）
cd /d E:\selftext\skillswap\admin-web
npm run build:deploy          :: = vite build + node ../scripts/copy-dist.js
cd /d E:\selftext\skillswap\cloudbase
tcb fn deploy admin-static --force -e nno-d2gspwvpl6c3c9f46
```

> 索引变更不在代码里，需在控制台操作（或用 MCP 的 `writeNoSqlDatabaseStructure` 的 `DropIndexes` / `CreateIndexes`）。

> `tcb fn deploy` 覆盖已存在的函数时会交互式询问，脚本化时加 `--force`。
> **不要加 `--httpFn`**：那是「Web 函数（容器）」类型，需要 `scf_bootstrap`；
> 本项目是普通事件函数 + HTTP 访问服务挂 `/skillswap-api`、`/skillswap-web/` 两条路径。

小程序端在开发者工具**重新编译**（新增了组件目录，建议「编译」前先「清缓存 → 全部清除」）。


